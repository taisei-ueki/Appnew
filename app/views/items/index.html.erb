<h4>item list</h4>
<div class="item_container">
  <% @items.each do |item| %>
    <%= link_to item_path(item.id) do %>
      <%= image_tag item.get_image, size: '100×100' %>
    <% end %>
    <%= item.name %>
    <%= item.price %>
  <% end %>
</div>